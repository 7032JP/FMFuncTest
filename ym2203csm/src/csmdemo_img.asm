; =============================================================================
; csmdemo_img.asm — 画像データだけを 1 本のバイナリにするためのラッパ
;
;   assets/fm77av_img.s (scripts/png2fm77av.py が生成) をそのまま raw で
;   アセンブルし、ディスクへ置くイメージを作る。並びは:
;
;     +$0000..+$013F  imgpal    アナログパレット 64 エントリ x 5 バイト
;                               ($FD30,$FD31,$FD32,$FD33,$FD34 の順)
;     +$0140          imgtxtcol 重ね書き文字の色 (パレット添字)
;     +$0141..        imgdata   RLE ストリーム (展開後 48000 バイト)
;
;   メインCPU 側 (csmdemo.asm) はこのバイナリをディスクから 1 セクタずつ
;   読みながら展開するので、メモリ上に丸ごと置く必要はない。
;
; ビルド: lwasm --6809 --format=raw -I assets -o csmdemo_img.bin csmdemo_img.asm
; =============================================================================

        org     $0000

        include "fm77av_img.s"

        end
